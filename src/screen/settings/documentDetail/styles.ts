import { StyleSheet } from "react-native";

const documentstyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    uploadDocument: { marginBottom: 30 }

})
export default documentstyles;